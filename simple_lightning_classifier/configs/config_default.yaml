data:
  test_size: 0.2
  random_state: 42
  standardize: true

model:
  hidden_dim: 16
  dropout: 0.1

training:
  max_epochs: 5
  batch_size: 32
  learning_rate: 0.001
  num_workers: 0
  log_every_n_steps: 10